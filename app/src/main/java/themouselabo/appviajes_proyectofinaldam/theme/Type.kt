package themouselabo.appviajes_proyectofinaldam.theme

import androidx.compose.material3.ButtonDefaults
import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

// Set of Material typography styles to start with
val Typography = Typography(
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Monospace,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        lineHeight = 24.sp,
        letterSpacing = 0.5.sp
    )
)

// PÁGINAS
val PageTittleTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 24.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageSubTittlesTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 22.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageTabTittlesTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageSettingsTittlesTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 0.8.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageSettingsOptionsTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 12.sp, // Tamaño de fuente
    letterSpacing = 0.8.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val PageButtonSize = 20.dp
val PageButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val TranslateButtonSize = 30.dp
val TranslateTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 26.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val TranslateButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 1.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)


// ESTILO DIALOGS
val DialogTittleTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 18.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val DialogTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 15.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val DialogDateButtonSize = ButtonDefaults.IconSize
val DialogDateTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 10.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val DialogButtonSize = ButtonDefaults.IconSize
val DialogButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

// ESTILO CARDS
val CardTittleTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CardTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CardDateTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CardHashtagTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CardButtonSize = 20.dp
val CardButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 35.sp // Altura de línea estándar
)

// DATES
val DayTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val MonthYearTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 10.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val DateTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val EmptyTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 0.8.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

// OTROS
val ButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 1.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CategoryTittleTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
val CategoryElementTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 14.sp, // Tamaño de fuente
    letterSpacing = 0.8.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

val SignInButtonTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Thin, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 0.8.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

val DestinationTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 26.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

val TittleTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.SemiBold, // Normalmente se usan en negrita
    fontSize = 20.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)

val BasicTextStyle = TextStyle(
    fontFamily = FontFamily.Monospace, // O puedes usar una fuente personalizada si lo deseas
    fontWeight = FontWeight.Light, // Normalmente se usan en negrita
    fontSize = 16.sp, // Tamaño de fuente
    letterSpacing = 1.25.sp, // Espaciado entre letras
    lineHeight = 20.sp // Altura de línea estándar
)
